#include "main.h"
/**
 * print_alphabet- the funcion prints the alphabets
 * Return Zero upon execution
 */
void print_alphabet(void)
{
	char i;

	for  (i = 'a' ; i <= 'z' ; i++)

		_putchar(i);
	_putchar('\n');
}

#include <stdio.h>
/**
 * main - This function prints the defined words
 *
 * Return: Always return zero if its a success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,/n");
	return (0);
}
